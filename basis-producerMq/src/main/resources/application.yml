server:
  port: 8500
spring:
  application:
    name: mq

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# 配置 rabbitmq 的交换机和路由 key
rabbitmq:
  exchange: order
  routingkey: orderRouting
